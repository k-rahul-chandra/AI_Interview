# OpenAI API Key for Speech-to-Text (Whisper), Text-to-Speech (TTS), and LLM evaluations.
# If left empty, the application will automatically fall back to browser-based 
# Web Speech APIs (SpeechRecognition and SpeechSynthesis) for free, zero-config operation.
OPENAI_API_KEY=your_openai_api_key_here
